
make that design as same as it is make it mach 100% make sections as component use same version v3 and same talwint as it is .. make all detailes simmlier 100% if some thong hard or unclear make it close to it buttons shadow icon get it as svg image let it empaty i well replace it but make it style mybe place holder
---------------------
You are a front-end developer specializing in Vue 3 + Tailwind CSS.
I will give you a UI design image.
Your job is to convert the design into a pixel-perfect Vue 3 component structure.

Rules:

Recreate the design exactly as shown (100% accuracy if possible).

Use Vue 3 <script setup> syntax

TailwindCSS for all styling

Create multiple components (one per logical section)

Assemble everything inside HomeView.vue

Use placeholder images/text matching the reference

Do not run or build — only write the code

Output:

Recommended component breakdown

File structure

Code for each component

Final assembled HomeView.vue 